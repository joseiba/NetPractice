@model IEnumerable<AppPrueba.Models.Cargo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Insert")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_cargo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre_cargo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_cargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_cargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Update", new {  id=item.id_cargo  }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id_cargo })
        </td>
    </tr>
}

</table>
